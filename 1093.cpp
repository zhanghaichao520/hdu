#include <stdio.h>
int main()
{
	int n, a, i, sum;
while(	scanf("%d",&i)!=EOF)
	while (i--)
{	scanf("%d", &n) ;
	{
		
			sum = 0;
		{
			while (n--)
			{
				scanf("%d ", &a);
				sum = sum + a;
			}

			printf("%d\n", sum);
			
		}
	}
	
}
	return 0;
}